# 集成指南

本文面向想把 EUI-NEO 接入自己 C++ 项目的用户。当前推荐四种方式，按易用程度排序：

- 公共头文件方式：用 `include/eui_neo.h` + 框架提供的 app main 快速创建完整桌面 app。
- 静态库方式：链接 `eui::neo`，接入已有 main、窗口或渲染循环。
- 安装包方式：先 `cmake --install`，再在外部项目中 `find_package(EuiNeo CONFIG REQUIRED)`。
- examples 开发方式：直接在 `examples/` 下新增应用，适合快速验证和内置示例。

EUI-NEO 通过 CMake 导出一个库 target：

```cmake
eui::neo
```

当 EUI-NEO 作为子目录接入时，默认不会构建仓库自带的 `gallery`、`eui_demo`、`serial_tool` 等示例程序。需要显式构建示例时，配置 `-DEUI_BUILD_APPS=ON`。

## 快速开始：接入一个最小 CMake 项目

如果你要在自己的项目里直接创建一个完整 EUI-NEO 桌面应用，最小结构如下：

```text
MyProject/
  CMakeLists.txt
  app.cpp
  external/
    EUI-NEO/
```

`CMakeLists.txt`：

```cmake
cmake_minimum_required(VERSION 3.14)
project(MyProject LANGUAGES C CXX)

set(CMAKE_CXX_STANDARD 17)
set(CMAKE_CXX_STANDARD_REQUIRED ON)

add_subdirectory(external/EUI-NEO)

add_executable(my_app
    external/EUI-NEO/core/app/glfw_app_main.cpp
    app.cpp
)

eui_neo_configure_app(my_app)
```

`app.cpp`：

```cpp
#include "eui_neo.h"

namespace app {

const DslAppConfig& dslAppConfig() {
    static const DslAppConfig config = DslAppConfig{}
        .title("My App")
        .pageId("my_app")
        .windowSize(960, 640)
        .fps(90.0);
    return config;
}

void compose(eui::Ui& ui, const eui::Screen& screen) {
    ui.column("root")
        .size(screen.width, screen.height)
        .padding(32.0f)
        .gap(12.0f)
        .content([&] {
            ui.text("title")
                .text("Hello EUI-NEO")
                .fontSize(28.0f)
                .lineHeight(34.0f)
                .build();

            components::button(ui, "primary")
                .text("Start")
                .onClick([] {
                    // update app state here
                })
                .build();
        })
        .build();
}

} // namespace app
```

构建：

```sh
cmake -S . -B build -DCMAKE_BUILD_TYPE=Release
cmake --build build --parallel
./build/my_app
```

Windows 多配置生成器通常这样运行：

```powershell
cmake -S . -B build
cmake --build build --config Release
.\build\Release\my_app.exe
```

这条路径默认使用 GLFW 窗口后端和 OpenGL 渲染后端。需要 Vulkan 时使用独立构建目录：

```sh
cmake -S . -B build-vk -DCMAKE_BUILD_TYPE=Release
```

需要 SDL2 窗口后端时，先把 app main 换成 `external/EUI-NEO/core/app/sdl2_app_main.cpp`，再配置：

```sh
cmake -S . -B build-sdl2 -DCMAKE_BUILD_TYPE=Release
cmake --build build-sdl2 --parallel
```

`eui_neo_configure_app(my_app)` 会完成三件事：链接 `eui::neo`、设置平台相关链接选项、把 EUI-NEO 的 `assets/` 复制到可执行文件目录。

### Shadertoy 接入

需要把 `mainImage()` fragment 作为 UI 图元时，先构建 `ShaderToyGraph`，再交给
`ui.shadertoy()`。文件和 inline source、多 Pass、图片、feedback 和自定义 uniform
使用同一个公共 facade：

```cpp
eui::ShaderToyGraph graph;
graph.addPass("bufferA", "shaders/buffer.frag",
              "generated/buffer.frag.spv");
graph.addInlinePass("image", INLINE_SHADER_SOURCE, INLINE_SPIRV,
                    "image-inline.frag");
graph.setChannel("bufferA", 0, eui::ShaderToyChannel::self());
graph.setChannel("image", 0,
                 eui::ShaderToyChannel::buffer("bufferA"));
graph.setChannel("image", 1,
                 eui::ShaderToyChannel::image("assets/noise.png"));
graph.setUniform("uAmount", 0.75f);

ui.shadertoy("effect")
    .size(640.0f, 360.0f)
    .resolutionScale(1.0f)
    .timeScale(1.0f)
    .paused(false)
    .resetKey(revision)
    .graph(std::move(graph))
    .build();
```

每个 Pass 固定四路 `sampler2D`，通道 kind 只有 Image、Buffer、Self、None。图片使用
linear/repeat 并在加载时垂直翻转，Buffer/Self/None 使用 linear/clamp-to-edge；这套
固定采样策略不提供公共配置。所有 Pass 使用同一
`图元尺寸 * resolutionScale` 和固定 RGBA32F ping-pong target。

较早 Pass 的 Buffer 引用读取当前 graph 帧，Self 以及相同或较晚 Pass 引用读取上一帧；
首帧尚无反馈时绑定 1x1 空纹理。最后一个 Pass 是图元输出。builder 还继承普通 shape 的
布局、祖先裁剪、圆角、opacity、transform、命中和 dirty rect 语义。鼠标只在图元区域内
更新；区域内开始的按下会捕获到释放。

Vulkan 的每个 pass 必须提供有效 SPIR-V；可直接填写已有产物，或在构建期生成。文件与
inline helper 都只存在于 Vulkan 配置，`UNIFORMS` 的名称和类型必须与 graph 一致：

```cmake
if(EUI_NEO_RENDER_BACKEND STREQUAL "vulkan" OR
   EUI_RESOLVED_RENDER_BACKEND STREQUAL "vulkan")
    eui_compile_shadertoy(my_app
        SOURCE "shaders/buffer.frag"
        OUTPUT "${CMAKE_CURRENT_BINARY_DIR}/generated/buffer.frag.spv"
        UNIFORMS "uAmount:float")

    set(inline_shader [[void mainImage(out vec4 c, in vec2 p) {
        c = texture(iChannel0, p / iResolution.xy);
    }]])
    eui_compile_shadertoy_inline(my_app
        CONTENT "${inline_shader}"
        OUTPUT "${CMAKE_CURRENT_BINARY_DIR}/generated/image.inline.spv")
endif()
```

OpenGL 由驱动直接编译文件或内存源码，不需要 SPIR-V。Vulkan helper 只在构建机调用
wrapper 和 SDK 编译器，运行时绝不启动或链接编译器。

版本化 JSON 可直接导入同一个 graph：

```cpp
eui::ShaderToyGraph graph;
eui::ShaderToyError error;
if (!eui::loadShaderToyGraphJson("assets/effect.json", graph, error)) {
    // error.stage == "graph-json" 时可读取 sourcePath 和 message。
}
```

EUI JSON 根对象使用 `version: 1` 和 `passes`。每个 pass 的 `name` 必填，`source` 与
`inlineSource` 二选一；还可设置 `sourceName`、`spirv` 和最多四路 channel。
可序列化的 channel kind 是 `none|image|buffer|self`；custom uniform 支持
`float|int|vec2|vec3|vec4`，最多 16 个。fragment、SPIR-V 和图片相对路径均相对于
JSON 文件目录。
机器契约见
[`assets/shaders/shadertoy/eui-shadertoy.schema.json`](../assets/shaders/shadertoy/eui-shadertoy.schema.json)。
旧数组式配置由 importer 兼容，但不属于 EUI v1 schema。

完整 API、默认值、错误模型和双端唯一兼容矩阵见
[Shadertoy 底层图元](Shadertoy.md)。仓库内可直接构建的 `shadertoy` target
展示 inline Demo 与 JSON 导入的 Blackhole、Fish。

Keyboard、Audio、Video、Sound、Cubemap、Volume、动态纹理/GPU import、YUV、可配置
sampler、每 Pass 格式/分辨率明确不属于当前范围，也不是后续待办。保持这条
边界可以让公共 API、两个渲染后端、文档和依赖体积持续同步。

## 1. 公共头文件方式

这是最简单的完整 app 接入方式。它不是纯 header-only：应用代码只包含公共 facade 头文件，构建仍然链接 EUI-NEO 的静态库并使用框架提供的 app main。

推荐先按上面的“快速开始”结构接入：

- `add_subdirectory(external/EUI-NEO)` 把 EUI-NEO 加到你的 CMake 构建里。
- `external/EUI-NEO/core/app/glfw_app_main.cpp` 提供默认 GLFW app main。
- `eui_neo_configure_app(my_app)` 链接 `eui::neo`、设置平台链接选项，并复制 `assets/` 到可执行文件目录。
- 应用只需要实现 `app::dslAppConfig()` 和 `app::compose()`，然后在 `compose()` 里用 DSL 和 components 写界面。

默认窗口后端是 GLFW。需要 SDL2 完整 app 时，配置 `-DEUI_WINDOW_BACKEND=sdl2`，并把 app main 换成 SDL2 版本：

```cmake
add_executable(my_app
    external/EUI-NEO/core/app/sdl2_app_main.cpp
    app.cpp
)
eui_neo_configure_app(my_app)
```

在你自己的项目里直接配置当前项目即可：

```sh
cmake -S . -B build-sdl2 -DCMAKE_BUILD_TYPE=Release
cmake --build build-sdl2 --parallel
```

这样默认字体、图标、图片和示例资源可以按 `assets/...` 路径找到。

## 2. 静态库方式

如果你已经有自己的 main、窗口系统、渲染 context 或事件循环，直接链接 EUI-NEO 导出的静态库 target：

```cmake
add_subdirectory(external/EUI-NEO)

add_executable(my_app main.cpp app.cpp)
target_link_libraries(my_app PRIVATE eui::neo)
eui_neo_copy_assets(my_app)
```

普通 UI 代码仍然优先包含公共 facade：

```cpp
#include "eui_neo.h"
```

只有接入边界需要包含底层 runtime / platform 头文件。比如嵌入已有 GLFW / OpenGL 主循环时，才会直接使用 `core::dsl::Runtime`。

也可以用 CMake `FetchContent` 拉取 EUI-NEO：

```cmake
include(FetchContent)

FetchContent_Declare(
    eui_neo
    GIT_REPOSITORY https://github.com/sudoevolve/EUI-NEO.git
    GIT_TAG        main
)
FetchContent_MakeAvailable(eui_neo)

add_executable(my_app main.cpp app.cpp)
target_link_libraries(my_app PRIVATE eui::neo)
eui_neo_copy_assets(my_app)
```

实际项目建议把 `GIT_TAG` 固定到 release tag 或具体 commit，避免上游变化影响构建复现。

## 3. 安装包 / find_package 方式

从源码安装：

```sh
cmake -S . -B build-install -DCMAKE_BUILD_TYPE=Release -DEUI_BUILD_APPS=OFF
cmake --build build-install --target eui_neo --parallel
cmake --install build-install --prefix /tmp/eui-install
```

外部项目：

```cmake
cmake_minimum_required(VERSION 3.14)
project(MyProject LANGUAGES CXX)

find_package(EuiNeo CONFIG REQUIRED)

add_executable(my_app main.cpp)
target_link_libraries(my_app PRIVATE eui::neo)
```

配置外部项目：

```sh
cmake -S . -B build -DCMAKE_PREFIX_PATH=/tmp/eui-install
cmake --build build --parallel
```

安装包方式适合把 EUI-NEO 作为系统或 CI 缓存中的独立依赖。当前 package 会导出 `eui::neo` target 和公共头文件；默认 GLFW/OpenGL 路径会继续通过 CMake 依赖 target 传播。SDL2 / Vulkan 仍需要消费方环境提供对应 SDK 或包。

## 4. 直接在 `examples/` 开发

快速实验、复现问题或新增内置 demo 时，可以直接在仓库的 `examples/` 下开发：

```text
examples/
  gallery.cpp
  eui_demo.cpp
  serial_tool.cpp
  my_app.cpp
```

`examples/my_app.cpp`：

```cpp
#include "eui_neo.h"

namespace app {

const DslAppConfig& dslAppConfig() {
    static const DslAppConfig config = DslAppConfig{}
        .title("My EUI App")
        .pageId("my_app")
        .windowSize(900, 600)
        .fps(90.0);
    return config;
}

void compose(eui::Ui& ui, const eui::Screen& screen) {
    ui.column("root")
        .size(screen.width, screen.height)
        .padding(32.0f)
        .gap(12.0f)
        .content([&] {
            ui.text("title")
                .text("Hello EUI-NEO")
                .fontSize(28.0f)
                .lineHeight(36.0f)
                .build();

            components::button(ui, "button")
                .text("Click")
                .onClick([] {
                    // update your app state here
                })
                .build();
        })
        .build();
}

} // namespace app
```

顶层构建会自动扫描 `examples/*.cpp`，并为每个文件生成同名可执行程序：

```sh
cmake -S . -B build
cmake --build build
./build/my_app
```

复杂示例建议参考 `gallery` 的组织方式：`examples/gallery.cpp` 只保留应用入口、全局主题、导航壳和页面路由；具体页面拆到 `examples/pages/*.h`。每个页面文件定义一个页面对象，例如 `GalleryControlsPage`，对象内部持有自己的交互状态，并提供 `compose(eui::Ui&, float width, float height)` 声明页面内容。这种方式接近 QML 的页面组件模型：入口负责装配和切换，页面自己管理自己的状态和画面。

```text
examples/
  gallery.cpp              # app shell, route, shared theme
  pages/
    gallery_controls.h     # struct GalleryControlsPage { state + compose(...) }
    gallery_bing.h
    gallery_about.h
```

`examples/pages/*.h` 是被 `gallery.cpp` 包含的页面组件头，不是独立编译单元。页面里可以直接使用 `gallery.cpp` 中的主题 token、动画 helper 和共享小工具；业务交互状态优先放在页面对象里，而不是继续堆在 `gallery.cpp` 的匿名 namespace。

因此使用 VSCode 的 Microsoft C/C++ 扩展单独打开 `examples/pages/*.h` 时，可能会因为看不到 `gallery.cpp` 中的主题 token、动画 helper 或共享函数而出现红线。这是 IntelliSense 上下文限制，不代表真实构建失败。建议以 CMake 构建结果为准；如果希望减少这类误报，可以改用 clangd，或直接忽略 `examples/pages` 下的 Microsoft C/C++ 红线。

测试 SDL2 后端时，单独开一个构建目录：

```sh
cmake -S . -B build-sdl2
cmake --build build-sdl2 --target serial_tool
./build-sdl2/serial_tool
```

## 嵌入已有 GLFW / OpenGL 主循环

如果你的程序已经自己创建了 GLFW 窗口和 OpenGL context，可以直接持有一个 `core::dsl::Runtime`：

```cpp
#include "core/dsl_runtime.h"
#include "core/input/input_state.h"

core::dsl::Runtime runtime;
runtime.initialize(window);

while (!glfwWindowShouldClose(window)) {
    glfwPollEvents();

    int framebufferWidth = 0;
    int framebufferHeight = 0;
    glfwGetFramebufferSize(window, &framebufferWidth, &framebufferHeight);

    float contentScaleX = 1.0f;
    float contentScaleY = 1.0f;
    glfwGetWindowContentScale(window, &contentScaleX, &contentScaleY);
    const float dpiScale = (contentScaleX + contentScaleY) * 0.5f;
    const float logicalWidth = static_cast<float>(framebufferWidth) / dpiScale;
    const float logicalHeight = static_cast<float>(framebufferHeight) / dpiScale;

    runtime.compose("embedded", logicalWidth, logicalHeight, [](eui::Ui& ui, const eui::Screen& screen) {
        ui.row("root")
            .size(screen.width, screen.height)
            .content([&] {
                ui.text("hello")
                    .text("Embedded EUI-NEO")
                    .position(24.0f, 24.0f)
                    .fontSize(22.0f)
                    .build();
            })
            .build();
    });

    const bool shouldRender = runtime.update(window, deltaSeconds, dpiScale, dpiScale, true);
    if (shouldRender) {
        runtime.render(framebufferWidth, framebufferHeight, dpiScale);
        glfwSwapBuffers(window);
    }
}

runtime.shutdown();
```

嵌入模式下，你需要自己负责：

- `glfwInit()` / `glfwTerminate()`。
- 创建 OpenGL 3.3 core profile context。
- 调用 `gladLoadGLLoader(...)`。
- 安装或转发输入事件。直接使用 EUI-NEO 的 `core/input/input_state.h` 时，可调用 `core::installInputCallbacks(window)`。
- 主循环节流、清屏、swap buffers 和窗口生命周期。

如果不需要完全自定义主循环，优先使用“公共头文件方式”。

Vulkan 安装包还导出 `eui::shadertoy_wrap`、`eui_compile_shadertoy()` 和
`eui_compile_shadertoy_inline()`。外部应用在 `find_package(EuiNeo CONFIG REQUIRED)` 后
可直接调用 helper，也可提供预生成 SPIR-V。只有实际生成 SPIR-V 的构建机需要 Vulkan
SDK `glslangValidator`；目标机只需要 Vulkan loader/driver 和产物。OpenGL 安装包不会
安装或探测这些 Vulkan 构建工具。

## 依赖和资源

EUI-NEO 的第三方依赖由 `3rd/dependencies.cmake` 处理：

- `EUI_DEPS_MODE=auto`：默认。GLFW / glad 依次尝试父项目已有 target、包管理器 target、本地 `3rd/` 和固定上游；SDL2 先查找系统包再拉取；其余 vendored 依赖优先使用本地 `3rd/`，缺失时从固定上游拉取。
- `EUI_DEPS_MODE=bundled`：严格离线，只使用 `3rd/`。
- `EUI_DEPS_MODE=fetch`：强制联网拉取。

`3rd/dependencies.cmake` 是 `3rd/` 顶层唯一由 EUI-NEO 维护的 CMake 文件。GLFW、glad、tray、FreeType、zlib、libpng 和 MD4C 会根据各自的标志头文件与核心源码自动识别，不依赖源码目录名和版本后缀。替换版本时直接把一个完整的上游源码文件夹放到 `3rd/` 的下一层即可，不需要修改配置；同一个库同时存在两个匹配目录时会明确报错，避免选错版本。各源码目录内部的 `CMakeLists.txt` 属于上游构建文件，仍需保留。

GLFW / glad 是主程序和 EUI-NEO 都可能直接接触的公共接口依赖。父项目如果已经提供了名为 `glfw` 或 `glad` 的 CMake target，EUI-NEO 会优先复用已有 target，不会再自动引入 `3rd/` 下的同名依赖；没有已有 target 时，才按 `find_package`、内置源码或 fetch 兜底继续解析。

作为子目录接入时，`EUI_ENABLE_INSTALL` 默认关闭，避免把父项目提供的公共依赖 target 混入 EUI-NEO 的安装导出；需要统一安装时由父项目显式开启并管理这些公共 target。

窗口后端默认是 GLFW。SDL2 是可选后端，不 vendored 到 `3rd/`：

- `cmake -S . -B build-sdl2`：优先使用系统 SDL2 包，找不到时下载固定版本 SDL2。
- `cmake -S . -B build-sdl2 -DEUI_DEPS_MODE=fetch`：下载固定版本 SDL2。
- `cmake -S . -B build-sdl2 -DEUI_DEPS_MODE=bundled`：直接报错。

内置和 fetch 下载的依赖默认按静态链接构建，包括 GLFW。Release 包不需要额外携带 GLFW DLL / dylib / so。只有使用系统 SDL2 包时，SDL2 仍可能按系统包提供的方式动态链接。

MD4C 默认开启，用于 `components::markdown(ui, id)` 解析 Markdown 文本。仓库默认 vendored 在 `3rd/md4c`，可通过 `-DEUI_ENABLE_MARKDOWN=OFF` 关闭；关闭后 Markdown 组件会退化为纯文本段落显示。

Markdown 组件会接入 MD4C 的 block/span/text 回调面，并用现有 DSL primitive 可视化 CommonMark 与启用的 MD4C 扩展：标题、段落、引用、列表、任务列表、分隔线、代码块、HTML block、表格、表格对齐、强调、加粗、链接/自动链接、图片、inline code、删除线、LaTeX math、wiki link、underline、inline HTML、entity、soft/hard break 等。当前组件不是浏览器内核：HTML/CSS 不会执行；inline code、链接、图片、math、wiki、HTML、删除线和下划线会在组件内部 line-box 排版中映射成可见文本、胶囊或装饰线，图片显示为占位文本。代码、引用、HTML block 和表格使用块级样式显示。

运行时资源默认按 `assets/...` 查找。`eui_neo_configure_app()` 和
`eui_neo_copy_assets()` 在源码/add_subdirectory 接入时复制完整 EUI `assets/` 树，因此
`assets/shaders/shadertoy/` 下的 preset 与 schema 也会一起复制，不会产生嵌套的
`shaders/shaders`。`eui::platform::resolveResourcePath("assets/...")` 会先检查当前工作目录，
再检查可执行文件旁的相对路径，因此下载后的 runtime 可以从任意工作目录启动。内置
Shadertoy 和 Gallery 的 Vulkan SPIR-V 会生成到随 runtime 打包的 assets 树；外部应用
使用 helper 生成的 SPIR-V 仍需按自己选择的输出路径部署。外部项目可以：

- 调用 `eui_neo_copy_assets(my_app)` 复制 EUI-NEO 自带资源。
- 在自己的构建系统中复制应用 JSON、fragment、图片和预生成 SPIR-V。
- 通过 `DslAppConfig::textFont(...)`、`iconFont(...)`、`iconPath(...)` 指向自己的资源。

当前 `cmake --install` 安装 SDK 不会自动安装仓库 `assets/`；find-package 消费方必须显式
部署所需 preset/schema 或自己的 Shader 资源。Shadertoy JSON importer 复用已链接的
`eui::json`/yyjson，图片 channel 复用现有 image decoder，运行时不增加大型 parser、
Shader 编译器或媒体依赖，公共 API 也不暴露 GL/Vulkan 对象。

默认文本字体和图标字体找不到 assets 资源时会尝试平台系统字体兜底，避免普通文本在单 exe 漏带 assets 时整段不可见。FontAwesome 图标的 codepoint 仍依赖兼容的图标字体；需要图标稳定显示时，发布包应携带默认 `assets/` 或通过 `.iconFont(...)` 指向可用字体。

## 适合公开给用户的最小承诺

- C++17。
- CMake 3.14+。
- 默认 OpenGL 3.3 core profile，或显式选择 Vulkan 1.0+ 后端。
- GLFW 默认窗口输入，SDL2 为可选构建后端。
- 两个渲染配置共享同一 Shadertoy graph/JSON/source/fixed-sampling/pass 契约；Vulkan 构建时提供 SPIR-V，运行时不需要 Shader 编译器。
- `eui::neo` 是库 target；示例程序是可选目标。
